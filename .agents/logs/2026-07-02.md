# AI Work Log - 2026-07-02

## 2026-07-02T23:11:47+10:00

### Prompt

> Add yq command to cog pre_bump_hooks to set version in apm.yml during version bumps

### Model

Composer via Cursor

### Actions

- Updated `cog.toml` `pre_bump_hooks` to bump `apm.yml` version with `yq` and format it via `treefmt`
- Applied the same change to `template/cog.toml` for generated projects

### Outcome

✅ `apm.yml` version now bumps alongside `package.json` during `cog bump`
