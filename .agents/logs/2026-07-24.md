# AI Work Log - 2026-07-24

## 2026-07-24T09:20:28+10:00

### Prompt

> add this https://github.com/numtide/llm-agents.nix
> also add this - https://github.com/natsukium/mcp-servers-nix/blob/main/docs/module-usage.md#using-flakes and configure https://github.com/utensils/mcp-nixos

### Model

gpt-5.4-mini via Copilot CLI

### Actions

- Reviewed the current flake and upstream docs for `llm-agents.nix`, `mcp-servers-nix`, and `mcp-nixos`
- Clarified the scope with the user: selected `claude-code` and `opencode`, then `filesystem` only for the generated MCP config
- Agreed on an implementation plan to add the package inputs, expose the agent tools in the dev shell, and generate MCP config files automatically
- Updated `flake.nix` to add `llm-agents.nix` and `mcp-servers-nix`, wire `claude-code` and `opencode` into the dev shell, and generate `.mcp.json`/`opencode.json` from `mcp-servers-nix`
- Updated `.gitignore` and `template/.gitignore` to ignore the generated MCP config files
- Removed the stale standalone `treefmt.nix` after folding treefmt config back into the flake
- Ran `nix fmt`, which updated `flake.lock` with the new inputs and formatted the Nix files
- Ran `nix flake check --no-write-lock-file` and confirmed the flake evaluates and all checks pass
- Started the `.treefmt.toml` → `treefmt.nix` migration by moving the formatter definitions into a Nix module and keeping the TOML file for now
- Left the markdown table sorter command as `smt` per the user’s instruction, rather than trying to substitute a package name

### Outcome

✅ Added the agent tools and MCP config wiring successfully

## 2026-07-24T11:56:46+10:00

### Prompt

> based on @[docs/human-plans/nix-devshell-redesign.md] create a new branch and implement it.
> use prek for git hooks.

### Model

Gemini 3.6 Flash via Antigravity

### Actions

- Created and checked out new branch `feat/nix-devshell-redesign`.
- Updated `cog.toml` scopes to include `"nix"`.
- Redesigned `treefmt.nix` with 32 standard program formatters (`actionlint`, `alejandra`, `beautysh`, `deadnix`, `djlint`, `dockerfmt`, `dockfmt`, `dos2unix`, `flake-edit`, `genemichaels`, `json-sort-cli`, `just`, `keep-sorted`, `nbstripout`, `nixf-diagnose`, `nixfmt`, `nixpkgs-fmt`, `prettier`, `ruff-check`, `ruff-format`, `shellcheck`, `shfmt`, `sqlfluff`, `sqlfluff-lint`, `statix`, `taplo`, `toml-sort`, `typos`, `typstyle`, `xmllint`, `yamllint`, `zizmor`) and custom formatters from `.treefmt.toml`.
- Updated `flake.nix` with:
  - `llm-agents.nix` packages (`rtk`, `antigravity-cli`, `copilot-cli`, `cursor-agent`, `opencode`, `apm`, `git-surgeon`) and binary cache.
  - `git-hooks.nix` configured with `prek` (`package = pkgs.prek;`) migrating `.pre-commit-config.yaml` (`treefmt`, `check-merge-conflicts`, `cspell`, `ggshield`, `forbidden-files`, `cocogitto`).
  - `mcp-servers-nix` generating MCP configs for `antigravity-cli`, `copilot-cli`, `cursor-agent`, `opencode`.
  - `agent-skills-nix` installing `awesome-copilot` `git-commit` skill to target harnesses (`.agent/skills`, `.github/skills`, `.cursor/skills`, `.opencode/skills`).
- Removed `.pre-commit-config.yaml` and updated `.gitignore` for generated MCP configs and local skill paths.
- Ran `treefmt -vv` and verified clean flake evaluation and devshell hook execution via `nix develop`.

### Outcome

✅ Successfully redesigned Nix devshell and configured prek git hooks, MCP servers, and agent skills.
